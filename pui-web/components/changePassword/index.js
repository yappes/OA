import PuiChangePassword from './src/changePassword.vue';

/* istanbul ignore next */
PuiChangePassword.install = function(Vue) {
  Vue.component(PuiChangePassword.name, PuiChangePassword);
};

export default PuiChangePassword;
